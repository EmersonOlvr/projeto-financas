# Tomcat port
server.port=8090

# Database configs
spring.datasource.url=jdbc:mysql://localhost/financas?useSSL=false
# ...&useTimezone=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=ifpe1234
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL5Dialect

# Hibernate ddl auto (create, create-drop, update)
spring.jpa.hibernate.ddl-auto=update

# Impede que o devtools restarte o programa toda vez que é feita uma nova alteração nas classes
#spring.devtools.restart.trigger-file=.reloadtrigger

# Mostra cada comando SQL que foi usado
spring.jpa.show-sql=false
